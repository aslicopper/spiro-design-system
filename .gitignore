node_modules/
*.log
.DS_Store
.env
.env.local
.tsbuildinfo

# Build outputs are committed for @aslicopper/tokens (so consumers can install
# without a build step). For @aslicopper/react they are not.
packages/react/dist/

# Preview build is generated by CI and shipped to GitHub Pages; never committed.
packages/preview/dist/
packages/preview/scripts/__pycache__/
